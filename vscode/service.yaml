apiVersion: v1
kind: Service
metadata:
  name: jlewi-vscode # {"$kpt-set":"name"}
spec:
  ports:
  - name: http-vscode
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: jlewi-vscode # {"$kpt-set":"name"}
  type: ClusterIP
