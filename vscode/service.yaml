apiVersion: v1
kind: Service
metadata:
  name: jlewicodeserver # {"$kpt-set":"name"}
spec:
  ports:
  - name: http-vscode
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: jlewicodeserver # {"$kpt-set":"name"}
  clusterIP: None
